from __future__ import annotations


class ClobClient:
    """CLOB API client placeholder (implemented in Step B / optional usage)."""

    def __init__(self, base_url: str) -> None:
        self.base_url = base_url
